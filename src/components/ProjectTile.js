const projectTile = (name, code) => {

    const temp_stop = ""
    return (
        <div className="container">
            <header className="jumbotron">
                <h3>here's the {this.props.content.name} and here's the {this.props.content.code}</h3>
            </header>
        </div>
    );
}

export default projectTile